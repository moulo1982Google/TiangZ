# 换机续接入口（2026-09-07）

## 最新续接状态

后续已提交 DBProxy `24d66b7`（提交后缓存修复清理移至既有维护 worker）、`4432808`（同库快照批次一次提交）及 `6104111`（测试健康等待覆盖 Compose 窗口）。前置 139 项默认、33 项真实数据库/故障及 7 项控制器测试通过。批次候选 r3 的 100 玩家启动及健康阶段通过，约第 23 分钟在 AOF 重启后因控制器只等待 10 秒而遇到 BusyLoading，未完成最终对账；原轮次 64 条和专项新 64 条事后完整恢复。控制器已改为等待所需 Redis 的真实健康状态，仍使用既有 90 秒窗口，完整 30 分钟待新候选提交后重跑，详见[尾延迟归因](latency-attribution.md)。下两段是更早的排队修复候选记录。

PG 排队控制已提交为 DBProxy `e094541`，默认排队 2 秒、失败重连冷却 500 ms。TiangZ 文档提交 `124dd7d` 后合并远端文档，验收冻结 HEAD 为 `34a4154`。双仓库 release、正常 Model/Hotfix、codegen 和探针构建通过；136 项默认测试、31 项真实数据库/故障测试及 7 项控制器/资源测试通过。

正式 30 分钟计划在北京时间 18:06:34 的 100 玩家启动健康检查失败，尚未进入计时负载、故障注入或最终对账。短时冒烟还复现了初始化时 PG 排队超时。两轮失败证据保留，机械盘同步与 SDK 队列数据见[提交后验收记录](../../../TiangZ-DBProxy/docs/pg-queue-acceptance-2026-09-07.md)。受管进程已回收、三只演练数据库容器健康；Docker 数据迁移尚未执行。

## 原换机交接状态（历史）

当前暂停在：**缓存预算拆分已完成，下一步 PG 故障连接排队控制尚未实施**。用户要求先整理并提交，换机器后继续，本次不启动新演练。

完整实施计划位于相邻 DBProxy 仓库 [docs/handoff-2026-09-07.md](../../../TiangZ-DBProxy/docs/handoff-2026-09-07.md)。它包含代码入口、排队/执行/结果未知边界、精确测试、30–60 分钟验收步骤、资源限制和证据转移注意事项。

续接顺序：

1. 同步两仓库本次提交，保持 `TiangZ/` 与 `TiangZ-DBProxy/` 同级；两边分支均为 `feat/generic-record-commit`。检查状态并保留用户新改动。
2. 读 `AGENTS.md`、两份 AI 文档及上述交接计划；不要重复实施缓存预算拆分，不把计划当已完成功能。
3. TiangZ `.cargo/config.toml` 和 npm 使用相邻 DBProxy 源码，不能只同步 TiangZ。Git 本地提交不等于已推送，换机前须确保提交已实际传过去。
4. 原始证据位于工作区 `.build-tmp/local-validation/20260907-cache-budget-30m-r13/`，不随 Git 迁移；配置凭据、Docker 数据卷及二进制也不会自动迁移。按需安全传输，不把这些内容提交仓库。
5. 新机器重新核对资源、独立测试数据库和缓存实际配置；先精确回归，再构建并启动新的唯一轮次。不得覆盖 r13 或清理真实业务数据库。

r13 的保存证据在交接时再次只读审计通过：100 游戏玩家 + 100 持久化探针，30 分钟、五次故障；200,196 次读取，旧读/缺失/不变量错误均 0；813 条事件和 AOF 64 条记录对账通过。保留所有暂时错误及一次恢复登录重试。详细统计与局限见[尾延迟归因](latency-attribution.md)第四步。

本次 TiangZ 只提交文档，不改业务、运行时、协议或生成物，不触碰 WoW335、不部署外网。PG 排队仍有秒级长尾；七天、完整发布门禁和 WoW335 实机验收不在本次完成范围。
