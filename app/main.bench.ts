// Bench 组合入口仅供静态工具；生产 Runtime 仍按 Model/Hotfix 双 Bundle 加载。 / Bench composition root for static tooling; production Runtime still loads separate Model and Hotfix bundles.
import "./model/main.bench";
