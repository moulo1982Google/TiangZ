// 显式加载行为；创建文件但不导入不会装配进运行时。 / Explicit behavior loading; merely creating a file does not install it.
import "./counter/CounterSceneSystem";
import "./counter/CounterComponentSystem";
import "./counter/handlers/IncrementHandler";
